---
import Card from '@/components/common/Card.astro';
import type { Service } from '@/data/services';

interface Props {
  service: Service;
}

const { service } = Astro.props;

const iconMap: Record<string, string> = {
  Radar: '📡',
  Cpu: '🔧',
  LineChart: '📈',
  Globe: '🌐',
  Monitor: '🖥️',
  Building: '🏛️',
};
---

<div id={service.id} class="scroll-mt-20">
  <Card>
    <div class="mb-4 flex items-start gap-4">
      <div class="text-3xl">{iconMap[service.icon] || '💻'}</div>
      <div>
        <h3 class="text-xl font-semibold text-content-primary">{service.title}</h3>
        <p class="mt-2 text-sm leading-relaxed text-content-tertiary">
          {service.description}
        </p>
      </div>
    </div>

    {/* Details */}
    {service.details.length > 0 && (
      <div class="mt-4">
        <p class="mb-2 text-sm font-medium text-content-secondary">
          {service.products ? '주요 내용' : '주요 내용'}
        </p>
        <ul class="grid gap-1.5 md:grid-cols-2">
          {service.details.map((d) => (
            <li class="flex items-start gap-2 text-sm text-content-tertiary">
              <span class="mt-1 text-content-interactive-primary">·</span>
              {d}
            </li>
          ))}
        </ul>
      </div>
    )}

    {/* Products */}
    {service.products && (
      <div class="mt-4">
        <p class="mb-2 text-sm font-medium text-content-secondary">주요 제품·기술</p>
        <div class="flex flex-wrap gap-2">
          {service.products.map((p) => (
            <span class="rounded-full bg-surface-info-subtle px-2.5 py-1 text-xs text-content-interactive-primary">
              {p}
            </span>
          ))}
        </div>
      </div>
    )}

    {/* Advantages */}
    {service.advantages && (
      <div class="mt-4">
        <p class="mb-2 text-sm font-medium text-content-secondary">장점</p>
        <ul class="space-y-1">
          {service.advantages.map((a) => (
            <li class="flex items-start gap-2 text-sm text-content-tertiary">
              <span class="mt-1 text-content-interactive-primary">·</span>
              {a}
            </li>
          ))}
        </ul>
      </div>
    )}

    {/* Applications */}
    {service.applications && (
      <div class="mt-4">
        <p class="mb-2 text-sm font-medium text-content-secondary">적용 분야</p>
        <div class="flex flex-wrap gap-2">
          {service.applications.map((a) => (
            <span class="rounded-full bg-surface-tertiary px-2.5 py-1 text-xs text-content-tertiary">
              {a}
            </span>
          ))}
        </div>
      </div>
    )}

    {/* Customers */}
    {service.customers && (
      <div class="mt-4">
        <p class="mb-2 text-sm font-medium text-content-secondary">고객 유형</p>
        <div class="flex flex-wrap gap-2">
          {service.customers.map((c) => (
            <span class="rounded-full bg-surface-tertiary px-2.5 py-1 text-xs text-content-tertiary">
              {c}
            </span>
          ))}
        </div>
      </div>
    )}
  </Card>
</div>
