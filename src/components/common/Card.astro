---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`rounded-xl border border-line-secondary bg-surface-primary p-6 transition-colors ${ className }`}>
  <slot />
</div>
