---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`rounded-xl border border-gray-200 bg-white p-6 transition-colors dark:border-gray-700 dark:bg-gray-800 ${ className }`}>
  <slot />
</div>
